# FlashLearn v5.52 — Progress / Statistics + E2E

## Scope
Complete the Progress/Statistics audit on the v5.51 baseline and close the end-to-end acceptance path from word creation through review history, progress, statistics, streaks, and inactive-concept filtering.

## Completed
- Progress aggregation verified against active Concepts and their LearningState/DifficultyState.
- Statistics aggregation verified from ReviewHistory: total reviews, correct/wrong counts, accuracy, and distinct reviewed concepts.
- Streak calculation verified by calendar day, including duplicate reviews on one day, streak breaks, and current-vs-longest behavior.
- Progress UI exposes learning-stage distribution, progress summary, review statistics, streaks, and achievements without changing learning/scheduling contracts.
- Refresh generation protection retained so stale asynchronous refreshes cannot overwrite newer state.
- Added a dedicated domain E2E contract covering Add Word → Review → persisted history → Progress/Statistics/Streak → soft delete.
- Runtime release identity advanced to 5.52/52.

## Invariants intentionally unchanged
- Learning Transition algorithm unchanged.
- Difficulty algorithm unchanged.
- Review scheduling/selection unchanged.
- Room schema remains version 5; no migration introduced.
- Library/Add Word/Bulk Import behavior from v5.51 retained.

## Verification
- Static source audit completed.
- Domain regression/E2E assertions added for Progress + Statistics + Streak consistency.
- Android runtime preflight gate aligned to versionName `5.52` and versionCode `52`.
- Final Android compile/instrumentation execution remains authoritative in GitHub Actions; this environment has no executable `gradlew` launcher and no system `gradle` command, so local Gradle execution was unavailable.
