# FlashLearn — Permanent Change Execution Protocol

این فایل «پروتکل ثابت اجرای تغییرات» پروژه FlashLearn است و باید برای **هر تغییر جدید** رعایت شود.

## 1. قانون اصلی

من فقط تغییر موردنظر را اعلام می‌کنم. اجرای کامل و نهایی تغییر بر عهده توست.

برای هیچ تغییری، حتی تغییر کوچک، فقط توضیح یا پیشنهاد ارائه نکن؛ تغییر را واقعاً در Repository اعمال کن و تا پایان فرآیند اعتبارسنجی ادامه بده.

## 2. بررسی قبل از تغییر

قبل از هر تغییر:

- وضعیت واقعی و فعلی Repository روی شاخه `main` را از GitHub بررسی کن.
- فایل‌ها و کدهای مرتبط را از نسخه فعلی بخوان.
- به نسخه‌های قدیمی، حافظه قبلی یا حدس تکیه نکن.
- اگر فایل ZIP یا سند مرتبط در اختیار است، محتوای واقعی آن را بررسی کن.
- وابستگی‌ها و اثر تغییر روی سایر قسمت‌های پروژه را بررسی کن.

## 3. اجرای تغییر

- تغییر را مستقیماً روی نسخه فعلی پروژه اعمال کن.
- تمام فایل‌های لازم را واقعاً اصلاح کن.
- اگر تغییر به اصلاح قسمت‌های دیگری نیاز دارد، آن اصلاحات را نیز انجام بده.
- کد موجود، قابلیت‌ها، الگوریتم‌ها، Database، UI، Accessibility، Localization و قراردادهای پروژه را بدون دلیل حذف یا خراب نکن.
- تناقض‌های ایجادشده یا موجود در محدوده تغییر را برطرف و نسخه نهایی را یکپارچه کن.

## 4. بررسی کامل بعد از تغییر

بعد از تغییر فقط همان فایل را بررسی نکن. اثر تغییر را در کل پروژه بررسی کن، از جمله:

- Compile errors
- Kotlin / Gradle errors
- Imports و References
- Navigation
- UI
- Theme / Design System
- Resources
- Strings / Localization
- Accessibility
- Database
- Algorithms / Learning Logic
- Tests
- CI
- Versioning
- Documentation
- وابستگی‌ها و فایل‌های تحت تأثیر

اگر چیزی خراب شد، همان مرحله اصلاحش کن و دوباره بررسی کن.

## 5. Build و CI

- GitHub Actions را بررسی کن.
- اگر CI شکست خورد، لاگ واقعی خطا را بخوان.
- حدس نزن.
- فایل مشکل‌دار را پیدا و اصلاح کن.
- سپس CI را دوباره بررسی کن.
- تا زمانی که آخرین اجرای مربوط به تغییر سبز نشده، کار را «تمام‌شده» اعلام نکن.
- Warning غیرمسدودکننده را از Error واقعی جدا کن.

روند اجباری خطا:

`Inspect → Identify exact error → Fetch affected file → Fix → Commit → CI → Recheck`

## 6. Versioning

هر تغییر نسخه‌ای که طبق ساختار پروژه لازم است باید در تمام منابع مربوطه هماهنگ شود.

حداقل این فایل‌ها را بررسی کن:

- `app/build.gradle.kts`
- `.github/workflows/android-ci.yml`
- `CHANGELOG.md`
- `README.md`
- `PROGRESS.md`
- `docs/VERSION_LEDGER.md`
- `docs/FlashLearn_PROGRESS_TRACKER.md`
- `docs/PROGRESS.md`
- `Standalone_Integrated_Specification_AUDITED.md`

هیچ فایل مستندی نباید نسخه قدیمی را به‌عنوان نسخه فعلی معرفی کند.

### مرجع شماره نسخه

منبع اصلی Runtime:

`app/build.gradle.kts`

منبع CI:

`.github/workflows/android-ci.yml`

مستندات باید با Runtime و CI هماهنگ باشند و نباید شماره نسخه را مستقل از این منابع تعیین کنند.

## 7. GitHub

پس از کامل شدن تغییر:

- تغییرات را روی Repository واقعی `https://github.com/manidigit/FlashLearn` اعمال کن.
- Branch اصلی `main` است.
- قبل از Update هر فایل، نسخه فعلی آن را بررسی کن تا تغییرات جدید overwrite نشوند.
- Commit مناسب ایجاد کن.
- SHA/Commit نهایی را ثبت و در گزارش اعلام کن.

## 8. عدم حذف بی‌دلیل

هیچ فایل، قابلیت، الگوریتم، مستند، تست یا کدی را صرفاً برای راحت‌تر شدن کار حذف نکن.

اگر حذف واقعاً ضروری است:

1. وابستگی‌ها را بررسی کن.
2. مطمئن شو قابلیت مهمی از بین نمی‌رود.
3. سپس حذف را انجام بده.
4. بعد از حذف، Build/Test/CI را دوباره بررسی کن.

## 9. معیار «انجام شد»

وقتی می‌گویی «انجام شد»، باید همه موارد زیر برقرار باشند:

- تغییر واقعاً در Repository اعمال شده است.
- کدهای مرتبط بررسی شده‌اند.
- اثرات جانبی بررسی شده‌اند.
- Build/Tests بررسی شده‌اند.
- GitHub Actions بررسی شده است.
- خطاهای واقعی برطرف شده‌اند.
- Versioning و Documentation هماهنگ هستند.
- Commit نهایی مشخص است.
- هیچ مورد Pending یا خرابِ شناخته‌شده‌ای به‌عنوان تکمیل‌شده اعلام نشده است.

اگر CI هنوز در حال اجراست، صریحاً بنویس:

**CI در حال اجراست — کار هنوز نهایی نشده است.**

## 10. گزارش نهایی

گزارش نهایی کوتاه و دقیق باشد:

**انجام شد**

- تغییرات اصلی:
- فایل‌های مهم تغییرکرده:
- نسخه:
- Commit:
- CI:
- خطای باقی‌مانده: ندارد / دارد + توضیح

اگر مشکلی باقی مانده، دقیقاً مشخص کن کدام مورد باقی مانده و چرا.

هرگز وضعیت ناتمام را کامل اعلام نکن.

## 11. قانون همیشگی

هر بار که یک تغییر جدید برای FlashLearn درخواست می‌شود، **تمام این فرآیند باید دوباره اجرا شود**؛ حتی اگر تغییر کوچک باشد.

کاربر نباید مجبور شود این مراحل را دوباره یادآوری کند.

این فایل مرجع دائمی فرآیند اجرای تغییرات پروژه است.
