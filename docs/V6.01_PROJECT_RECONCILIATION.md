# FlashLearn v6.01 — Project Source / Version / CI Reconciliation

## Canonical version
- versionName: **6.01**
- versionCode: **101**
- previous application version: **6.00 / 100**

Historical v4.33–v4.39 labels are specification-stage identifiers, not current Android application versions.

## Artifact naming correction
`FlashLearn_v4.39_FINAL_RELEASE_CERTIFICATION.zip` contains a certification/source package whose internal history legitimately includes v4.39. Its filename does not define the current app version.

`gtp.zip` is a separate source snapshot whose runtime identity is 6.00 / 100.

These are distinct artifacts and must remain distinct in the project ledger.

## v6.00 product checkpoint
The v6.00 Quiz contract remains:
- all target-language translations are grouped into each Quiz option;
- deterministic translation ordering is retained;
- answer feedback remains visible for 3 seconds;
- the next card advances automatically;
- manual continuation is not used.

The current Review source also contains Spanish TTS playback for Quiz questions; this is now explicitly logged.

## Verification rule
A documentation PASS is not a runtime PASS. A workflow file is not evidence of a successful build.

v6.01 is green only after Build, Unit, APK verification, Instrumentation, and the previous-version upgrade gate all pass.

## Bookkeeping rule
Every application-version change must update in the same commit:
- app/build.gradle.kts
- RuntimeGatePreflightTest
- GitHub Actions version gate
- CHANGELOG.md
- PROGRESS.md
- docs/FlashLearn_PROGRESS_TRACKER.md
- docs/VERSION_LEDGER.md

## Source-reconciliation boundary
The uploaded v4.39-named source package and the v6.00 source snapshot are not byte-identical. This checkpoint records that fact instead of silently selecting one snapshot and treating the histories as identical.

Historical implementation claims will only be promoted to current-source status after explicit source reconciliation and CI/test evidence.
