# Backlog Drafts

Early, unwired sketches for future work. Not part of any Gradle source set
and not aligned with the current final spec — keep only as loose inspiration
when actually building these features.

- `Achievement.kt` — rough draft for the Gamification/Achievements system
  (real target design: FIRST_TEN_WORDS, SEVEN_DAY_STREAK, THIRTY_DAY_STREAK,
  MEMORY_BUILDER, VOCABULARY_BUILDER, HARD_MODE_MASTER, LONG_TERM_MEMORY —
  see project discussion history, not reflected in this draft).
- `Progress.kt` / `Statistics.kt` — superseded; the real, working equivalents
  already exist as `GetProgressSummaryUseCase` in
  `domain/src/main/java/com/flashlearn/domain/usecase/SettingsDifficultyProgressUseCases.kt`.
